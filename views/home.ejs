<%- include('partials/header') %>

<h2>Bem-vindo ao Sistema de Busca</h2>
<% if (typeof sucesso !== 'undefined' && sucesso) { %>
    <p style="color:green;"><%= sucesso %></p>
<% } %>

<% if (typeof erro !== 'undefined' && erro) { %>
    <p style="color:red;"><%= erro %></p>
<% } %>

<h3>Cadastrar Website</h3>
<form method="POST" action="/websites/cadastrar">
    <input type="text" name="url" placeholder="URL do site" required><br>
    <input type="text" name="palavras" placeholder="Palavras-chave (separadas por vírgula)" required><br>
    <button type="submit">Cadastrar</button>
</form>

<h3>Buscar Websites</h3>
<form method="GET" action="/websites/buscar">
    <input type="text" name="palavra" placeholder="Digite uma palavra-chave" required>
    <button type="submit">Buscar</button>
</form>

<a href="/logout">Sair</a>

<%- include('partials/footer') %>